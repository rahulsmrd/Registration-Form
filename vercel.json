{
    "builds": [{
        "src": "user_models/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "user_models/wsgi.py"
        }
    ]
}
